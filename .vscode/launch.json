{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Cookie Server",
            "type": "go",
            "request": "launch",
            "mode": "auto",
            "program": "${workspaceFolder}/cmd/main.go"
        }
    ]
}